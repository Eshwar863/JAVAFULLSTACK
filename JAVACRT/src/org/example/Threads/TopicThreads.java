package org.example.Threads;

public class TopicThreads {
    /// process ---> it will have separate memory ,cpu ,ram and etc
    /// weighted component
    /// process is madeup of org.example.Threads

    /// thread ----> here the memory is shared among the another threads
    /// lightweight component
    /// independent so no other can effect
    /**
     * new Thread()
     * new ---> Start();
     * run()
     * Sleep() or wait()
     * distory() dead
     *
     *
     * here the start() is used to execute the thread which calls run() [overriden method]
     * */

    ///
}
